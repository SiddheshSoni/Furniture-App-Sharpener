This is a Furniture React App.
The admin dashboard and user app seperated so users can't access the dashboard 
to use the app make sure to setup a .env file with VITE_API_KEY="your google Web API Key" and VITE_BASE_URL="DB access url"
